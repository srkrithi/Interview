package array;

public class BinarySearch {

	//finds an element in a sorted array
	//performance - worst case O(logn) best case O(1)
	
	
//	while (low <= high) {
//        int mid = (low + high) / 2;
//        if (sortedArray[mid] < key) {
//            low = mid + 1;
//        } else if (sortedArray[mid] > key) {
//            high = mid - 1;
//        } else if (sortedArray[mid] == key) {
//            index = mid;
//            break;
//        }
//    }
//    return index;
}
